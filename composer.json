{
    "name": "noah-medra/prompt-builder",
    "description": "Compose and execute structured AI prompts in PHP with a fluent, framework-agnostic API (Laravel-friendly).",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Nouha DRAME",
            "email": "noudrahame97@gmail.com"
        }
    ],
    "autoload": {
      "psr-4": {
        "NoahMedra\\PromptBuilder\\": "src/"
      }
    },
    "require": {
      "php": "^8.1",
      "illuminate/support": "^10.0 || ^11.0 || ^12.0",
      "guzzlehttp/guzzle": "^7.0"
    },
    "require-dev": {
      "phpunit/phpunit": "^9.6",
      "laravel/pint": "^1.0",
      "orchestra/testbench": "^8.0 || ^9.0"
    },
    "autoload-dev": {
      "psr-4": {
        "Tests\\": "tests/"
      }
    },
    "extra": {
        "laravel": {
            "providers": [
                "NoahMedra\\PromptBuilder\\PromptBuilderServiceProvider"
            ],
            "aliases": {
                "PromptBuilder": "NoahMedra\\PromptBuilder\\Facades\\PromptBuilder"
            }
        }
    },
    "config": {
        "sort-packages": true,
        "audit": {
            "ignore": {
                "PKSA-m5cs-t1y6-qpcs": "Dev-only advisory via orchestra/testbench -> laravel/framework. The 'temporary signed URL' feature is not used by this library, which only requires illuminate/support. Unpatched on Laravel 10 (EOL), which our PHP 8.1 + PHPUnit 9 dev toolchain pins.",
                "PKSA-3r5d-mb8f-1qw9": "Dev-only advisory via orchestra/testbench -> laravel/framework. The email validation rule is not used by this library. Same Laravel 10 EOL toolchain constraint.",
                "PKSA-mdq4-51ck-6kdq": "Dev-only advisory (CVE-2026-48019) via orchestra/testbench -> laravel/framework. The email validation rule is not used by this library. Same Laravel 10 EOL toolchain constraint."
            }
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
